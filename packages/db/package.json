{
  "name": "@sofi/db",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'db: nothing to build'",
    "lint": "echo 'db: nothing to lint'",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "dotenv": "^16",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4"
  }
}
